# SYNTHETIC prices, not real market data

These files are generated by `examples/make_example.py` (a seeded random walk) so the
example portfolio runs offline and reproducibly. The tickers are real funds but the
prices are made up. Don't draw any conclusions from them.
